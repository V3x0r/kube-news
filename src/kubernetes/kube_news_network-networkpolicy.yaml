apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: kube_news_network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/kube_news_network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/kube_news_network: "true"
